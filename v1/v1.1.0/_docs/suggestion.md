# 💡 Suggestions — v1.1.0+

## High Priority
1. **PWA** — manifest.json + sw.js। Linkivo থেকে service worker pattern নাও।
2. **Resume download** — assets/ এ PDF রাখো, hero CTA তে "Download CV" button।
3. **GitHub API** — Projects এ live star count + last updated ফেচ করো।
4. **Admin panel** — `/admin` route, `auth.uid === YOUR_UID` check, contact messages view।

## Design
5. **Scroll progress bar** — top এ thin accent-colored bar।
6. **Custom cursor** — trailing glow effect on desktop।
7. **Counter animation** — hero stats already আছে, services stats এও করো।

## SEO
8. Dynamic `<title>` already router এ আছে — meta description ও per-page করো।
9. `robots.txt` + `sitemap.xml` add করো।
10. Open Graph image — proper 1200×630 OG image বানাও।

## Security
11. **Contact form rate limit** — already 60s আছে। Firebase Functions দিয়ে server-side rate limit করো।
12. **CSP header** — firebase.json এ Content-Security-Policy header add করো।

## Analytics
13. Custom events already আছে — Google Analytics dashboard এ goals set করো।
14. Microsoft Clarity (free heatmap) add করতে পারো।

## Content
15. **Testimonials section** — client/teacher feedback হলে add করো।
16. **Open to work badge** — hero তে "Available for freelance" toggle।
